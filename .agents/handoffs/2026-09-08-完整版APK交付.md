# Handoff: 完整版（签名 release）APK 交付（2026-09-08 00:16）

- 交接人 / 接手人：zcode-main / 待定（真机验收人）

## 改了什么

- `app/build.gradle.kts`：新增 release signingConfig（读 `keystore.properties`，缺失回退未签名）
- `.gitignore`：排除签名凭据（keystore.properties / keystores/ / *.jks / *.keystore）
- 本地生成自签密钥 `keystores/shiguang-release.jks`（10000 天，不入库）
- 认领文件已释放；commit 范围：`2cd013f..本次提交`

## 当前状态

- 全链 A→B→C **PASS** @ run `20260908_0013_release_001`
- 交付物：`app/build/outputs/apk/release/app-release.apk`（6.35MB，v1 方案签名，apksigner 校验通过，四 ABI）

## 已知问题

- minify 未开启（稳定性优先）；真机运行验收待设备
- 密钥与凭据仅存本机：`keystore.properties`、`keystores/shiguang-release.jks`——换机打包前需线下同步，否则同应用两把签名无法覆盖安装

## 如何验证我做的事

```bash
java -jar "$ANDROID_HOME/build-tools/35.0.0/lib/apksigner.jar" verify --print-certs app/build/outputs/apk/release/app-release.apk
# 预期输出：Signer #1 certificate DN: CN=ShiGuang Release ... 且退出码 0
./gradlew testDebugUnitTest assembleDebug assembleRelease
# 预期输出：BUILD SUCCESSFUL；测试 12/12
```

## 下一个 Agent 的第一步

1. `git pull` → 读 `.agents/STATE.md` + `pipeline_state.json`
2. 真机安装 release APK 验收后，把结论追加进 `.agents/DECISIONS.md`
