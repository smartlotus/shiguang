# Handoff: v1.1.1 输入法遮挡修复（2026-09-08 23:10）

- 交接人 / 接手人：zcode-main / 待定（真机验收人）

## 改了什么

- `ui/EditScreen.kt`：根 Column 加 `.imePadding()`（enableEdgeToEdge 下 API 30+ adjustResize
  失效导致键盘遮底的根因修复），配合既有 verticalScroll，聚焦输入框自动滚入可视区
- `app/build.gradle.kts`：versionCode 3 / versionName 1.1.1
- 认领已释放；commit 范围：`3c4a59b..本次提交`

## 当前状态

- 全链 A→B→C **PASS** @ run `20260908_2306_imefix_001`（一次通过）
- 交付物：`app/build/outputs/apk/release/app-release.apk`（6.37MB，v1.1.1，同钥可覆盖安装）

## 已知问题

- 键盘弹出后输入框滚入可视区为运行时行为，静态验收无法覆盖，**需真机确认**：
  编辑页点"备注"→ 弹键盘 → 备注框与"保存"按钮应可见、页面可滚动

## 如何验证我做的事

```bash
./gradlew testDebugUnitTest assembleDebug assembleRelease   # 预期 BUILD SUCCESSFUL，12/12
$ANDROID_HOME/build-tools/35.0.0/aapt dump badging app/build/outputs/apk/release/app-release.apk | grep versionName
# 预期：versionName='1.1.1'
```

## 下一个 Agent 的第一步

1. `git pull` → 读 `.agents/STATE.md` + `pipeline_state.json`
2. 真机验收键盘行为，结论追加 `.agents/DECISIONS.md`
