# Handoff: v1.1 动效/UI/小组件优化交付（2026-09-08 01:45）

- 交接人 / 接手人：zcode-main / 待定（真机验收人）

## 改了什么

- 动效：MainActivity 导航 AnimatedContent（层级滑动）、HomeScreen animateItem、
  ClassicStyle 入场缩放、DominoStyle 待倒摇摆、BarStyle 流光、DetailScreen 样式切换淡入缩放
- UI：EventsViewModel null=加载中（防空态闪现）、HomeScreen 加载小圆点 + 空态多米诺插画
- 小组件：深链直达详情（singleTop + EXTRA_OPEN_EVENT_ID + Uri 区分 PI）、
  “就是今天”前缀与日期后缀、4×2 新增 w_percent 百分比
- 版本：versionCode 2 / versionName 1.1；commit 范围：`2cd013f..本次提交`

## 当前状态

- 全链 A→B→C **PASS** @ run `20260908_0139_optimize_001`（attempt-2；attempt-1 编译失败已记录）
- 交付物：`app/build/outputs/apk/release/app-release.apk`（6.37MB，v1.1，与 v1.0 同钥可覆盖安装）

## 已知问题

- 动效手感与深链为运行时行为，待真机体验；minify 仍关闭

## 如何验证我做的事

```bash
./gradlew testDebugUnitTest assembleDebug assembleRelease   # 预期 BUILD SUCCESSFUL，12/12
java -jar "$ANDROID_HOME/build-tools/35.0.0/lib/apksigner.jar" verify app/build/outputs/apk/release/app-release.apk  # 预期退出码 0
$ANDROID_HOME/build-tools/35.0.0/aapt dump badging app/build/outputs/apk/release/app-release.apk | grep versionName
# 预期：versionName='1.1'
```

## 下一个 Agent 的第一步

1. `git pull` → 读 `.agents/STATE.md` + `pipeline_state.json`
2. 真机安装 v1.1，把动效/深链体验结论追加进 `.agents/DECISIONS.md`
